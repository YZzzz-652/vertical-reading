# Vertical Reading · README

## 你是谁

你是这个项目的AI协作者。项目owner是一位产品经理，不会写代码，用vibe coding方式独立开发。你需要阅读以下文档后再开始工作。

---

## 必读文档

每次对话开始时，根据本次任务类型阅读对应文档：

| 文档 | 内容 | 什么时候读 |
| --- | --- | --- |
| `VerticalReading_项目启动文档.md` | 产品定位、界面结构、技术栈、当前 Roadmap、当前待办表格 | 每次必读 |
| `VerticalReading_决策日志.md` | 历史决策和否定方案 | 每次必读，避免重复提已否定的方案 |
| `VerticalReading_Database_启动文档.md` | 书目清单、数据字段规范、提取规则 | 本次任务涉及数据生产时读 |

---

## 当前两条工作线

**工作线A · 数据生产**
从世界名著中提取结构化事件节点，生成CSV文件供用户导入飞书多维表格。
→ 阅读：项目启动文档 + 决策日志 + 数据库启动文档

**工作线B · 前端开发**（当前主线）
基于飞书数据构建可视化界面（时间轴+地图+人物面板），最终部署到云端。
→ 阅读：项目启动文档 + 决策日志

---

## 对话结束前你必须做的事

执行 `/review` 回顾本次对话，然后执行 `/update` 生成标准文档更新指令。

`/update` 输出格式如下，只输出本次对话涉及的【】块：

## Vertical Reading 文档更新指令
请按以下内容更新文档并推送到 GitHub，每个文件单独一次 commit。

【VerticalReading_决策日志.md】
在「[对应分类]」下新增：
[日期] · [标题]
- 决定：
- 原因：
- 否定方案：（无则不写）

【VerticalReading_项目启动文档.md】
将「[段落名]」部分更新为：
[新内容]

【VerticalReading_Database_启动文档.md】
在书目表中：
- 将《书名》状态改为 ✅已完成，条数填 XX 条

完成后列出每个文件的 commit hash，确认 push 成功。

用户将此指令复制发给 Codex 执行，Codex 负责修改文件并推送到 GitHub。

---

## 本地开发注意事项

项目存放在 OneDrive 目录，Turbopack 不兼容，本地启动必须使用：
```
npm run dev
```
（已在 package.json 中固定 --webpack，直接运行即可）

---

## 重要约束

* 飞书App Secret是敏感信息，不能出现在任何代码文件或前端页面中，只能用环境变量
* 数据字段结构已定型，不要修改飞书表格字段名
* 单选字段的值必须严格匹配数据库启动文档中的合法值列表
* 有任何不确定的决策，先问用户确认，不要自行决定
