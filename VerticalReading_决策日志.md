# Vertical Reading · 决策日志

## 使用说明

每次对话结束前，让AI把本次对话中做出的决定追加到对应分类下。格式：日期 + 决定内容 + 原因 + 否定的方案。

---

## 产品决策

**2026-04-25** · MVP聚焦时间维度穿透
* 决定：三条穿透维度中，MVP只做时间维度
* 原因：时间维度数据最明确，最容易跑通完整流程
* 后续：地点穿透、角色类型穿透作为后续迭代

**2026-04-25** · 虚构/模糊时间作品纳入产品
* 决定：《红楼梦》《百年孤独》等用"虚构映射"方式纳入时间轴
* 原因：真实历史与虚构世界并置本身是产品魅力
* 否定方案：只收录时间明确的作品

**2026-04-25** · 不纳入诗集
* 决定：诗集不纳入MVP数据范围
* 原因：没有虚构人物和情节事件，数据结构与小说不兼容
* 后续：可单独设计诗歌模块

**2026-04-25** · 产品当前阶段供个人使用
* 决定：不做用户登录和权限系统
* 后续：面向公众开放时再考虑

**2026-05-11** · 产品名称分层
* 决定：项目内部和文档统一称 Vertical Reading；网页对外展示名为 Parallels（中文：经纬）
* 原因：Vertical Reading 不够文艺，不适合作为产品对外名称；但所有项目文档已以 Vertical Reading 命名，内部继续使用，迁移成本高
* 否定方案：把项目和文档一起改名为 Parallels（迁移成本高、历史追溯麻烦）

**2026-05-12** · 设计规范派生色放开
* 决定：设计规范中色彩从"严格三色"放开为"三主色 + 可自然派生中间色"。主色仍为 parchment `#F5EDD8` / ink `#2C1A0E` / bronze `#c8a87a`；派生色用于副标题、meta 文字、分隔线、不同透明度叠加面板等场景，不作主色使用；命名建议体现"从哪一主色派生 + 用途"（如 `--fg-muted` 副标题用、`--fg-soft` meta 文字用）
* 原因：扫描发现实际 UI 已经存在多种 ink 附近派生色，严格三色禁令脱离实际；放开后系统更灵活，仍保持视觉克制
* 否定方案：严格限制三色不扩展（太死板，实际 UI 需要派生中间色）

**2026-05-12** · 顶部 banner 加点击返回首页
* 决定：地图页顶部 banner 的品牌字 "Parallels · 经纬" 整体可点击，点击触发 Landing 屏切换，带 0.6s 淡入淡出动效；不加任何视觉提示（无 hover 状态、无返回箭头），仅 cursor 变 pointer
* 原因：当前进入地图页后没有返回入口；按设计规范"无 hover tooltip、无视觉装饰"的硬规则保持克制
* 否定方案：加视觉返回箭头或 hover 提示（破坏极简气质）

**2026-05-12** · 地图年代切换不与时间轴联动
* 决定：地图年代切换功能由用户手动选择历史地图，不随时间轴自动切换；重写阶段先做 UI 框架（17/18/19/现代四个按钮可切换 active 状态，默认选中"现代"），按钮点击暂时无实际效果，等历史地图底图调研待办推进后再接真实底图
* 原因：自动联动复杂度高、用户失控；手动选择给用户主动权，技术实现简单
* 否定方案：随时间轴自动切换不同年代历史地图（实现复杂、用户体验失控）

**2026-05-12** · 筛选面板组件 FilterPanel 删除
* 决定：重写阶段彻底删除当前的 FilterPanel 组件，所有筛选条件（阶层、性别、时间类型、地点类型）挪到右侧新的"图志 Atlas"面板；左侧改为"事件列表卡片"，含"书中人物 Novels / 真实世界 History"双 tab
* 原因：Claude Design 的新交互模式更符合产品定位（左列表+右筛选+中地图三栏视图）；旧筛选面板与新设计语言不兼容
* 否定方案：保留 FilterPanel 隐藏但不删（代码堆积，违反"重开"原则）

**2026-05-12** · 左侧事件列表"代表事件"逻辑
* 决定：左侧"书中人物"tab 展示的事件，每本书在当前时间轴范围内取一条代表事件，规则为"书中位置 = 开头"且在时间范围内的事件
* 原因：飞书表里现在没有 "is_main_event" 字段，用现有"书中位置"字段做代理，最小数据改动；后续如发现选出来的事件不对劲，再考虑回飞书表加专用字段
* 否定方案：在飞书表里加"是否代表事件"字段并回填 16 本已完成的书（数据生产成本高，先用现有字段试跑）

---

## 前端交互

2026-05-16 · 真实世界历史事件数据存储方案
- 决定：硬编码进 app/world-events.ts，不存飞书
- 原因：数据只生产一次、较固定，无需飞书动态增删能力；零网络请求更快；字段可完全自定义
- 否定方案：存飞书（多一次API调用，字段与书籍事件不统一，维护成本高）

2026-05-16 · History tab 交互与联动方式
- 决定：纯文字列表展示，点击无响应；随时间轴区间联动筛选
- 原因：历史事件无地图坐标，点击无法触发有意义的地图联动；全量展示信息量过大，失去时间轴语境意义
- 否定方案：点击展开描述（信息已完整展示，展开无必要）；始终显示全部事件不联动（信息噪音过大）

2026-05-17 · 列表与地图 Marker 选中联动方案
- 决定：选中态全局唯一；点击列表触发 flyTo（1.2秒动画）并加光晕；点击 Marker 同步高亮列表；叉掉气泡=取消选中；光晕用白色半透明
- 原因：选中态唯一保证视觉焦点清晰；flyTo 动画提供空间感；气泡关闭作为取消入口符合用户直觉
- 否定方案：多选中态叠加（视觉混乱）；直接跳转无动画（空间感丢失）

---

## 前端开发

**2026-05-16** · 历史地图切换不联动时间轴，由用户手动触发
- 决定：图志面板「地图年代」选项点击触发底图切换，时间轴不参与联动
- 原因：两者是独立维度，不应耦合
- 否定方案：时间轴自动联动底图切换（最初指令方向错误，对话中纠正）

**2026-05-16** · 历史地图瓦片改为静态模板硬编码
- 决定：将 TileJSON tiles[0] 的真实瓦片 URL 直接写入 historical-maps.ts，不在运行时 fetch TileJSON
- 原因：服务端代理被 Cloudflare 403 拦截；浏览器端运行时 fetch 不稳定
- 否定方案：服务端 API 代理（/api/tilejson）、运行时客户端 fetch TileJSON

**2026-05-16** · 配准质量不佳的历史地图暂时隐藏，待专题解决
- 决定：16/17/18/19世纪底图加 hidden: true 暂时不展示，代码保留；在专题解决之前只保留现代和20世纪
- 原因：OldMapsOnline 上这4张图地理配准质量不足，表现为斜向偏移；穷举 Leaflet 参数调整、GeoTIFF 切片、David Rumsey 控制点调整均无法根治；所有排查过程已有完整文本记录
- 否定方案：继续调 Leaflet minzoom/maxzoom/tms/bounds、GeoTIFF 精确叠加、引入 OpenLayers/Allmaps

---

## 数据生产

2026-05-16 · 飞书数据密度清洗
- 决定：以「书名+角色名+显示年份」为单位，同组超过2条的由Claude判断保留转折点/起点，删除过程细节；共删除74条，494条→420条
- 原因：部分角色单年事件密度过高（最高10条），导致列表严重冗余，影响用户体验
- 否定方案：前端去重只保留第一条（治标不治本）；用户手动删除（工作量不可接受）；Codex自动判断重要性（需要文学理解能力，不适合脚本处理）

---

## 数据决策

**2026-04-25** · 数据存储选飞书多维表格
* 决定：用飞书多维表格存储，通过飞书开放API供前端调用
* 原因：操作门槛低，支持多视图管理，API完善
* 否定方案：Google Sheets（功能较弱）、直接用数据库（部署复杂度高）

**2026-04-25** · 数据提取方式选AI提取
* 决定：由Claude直接从书目知识中提取结构化事件节点
* 否定方案：爬虫（版权问题、工程复杂）

**2026-04-25** · 译本规范
* 决定：俄国文学用人民文学出版社译本，英美/法国文学用译林出版社译本

**2026-04-25** · 阶层标签用两层结构
* 决定：第一层大类标签（固定词表）+ 第二层细描标签（自由文本）
* 否定方案：只用单一标签（无法跨文化统一）

**2026-04-25** · 经纬度拆分为两个字段
* 决定：经度和纬度分开存储为两个数字字段

---

## 技术决策

**2026-04-25** · CSV自动写入工具
* 决定：用本地Node.js服务做中转，调用飞书API写入数据
* 否定方案：手动导入CSV（飞书不支持追加导入）、浏览器直接调用API（CORS报错）

**2026-04-25** · App Secret用环境变量存储
* 决定：App Secret只通过环境变量传入，不写入代码文件

**2026-04-25** · 部署目标为云端
* 决定：部署在Vercel，通过URL访问
* 否定方案：只在本地运行、Railway

**2026-04-25** · 文档管理选GitHub
* 决定：项目文档托管在GitHub公开仓库
* 否定方案：OneDrive（robots.txt限制，AI无法fetch）

**2026-04-25** · 建立四层文档体系
* 决定：README + 项目启动文档 + 决策日志 + 数据库启动文档

**2026-04-26** · 前端框架选Next.js
* 决定：使用Next.js作为前端框架
* 原因：免费、新手友好、内置服务端API能力、Codex支持最好、Vercel无缝部署
* 否定方案：其他框架（Codex支持质量较低）

**2026-04-26** · 地图组件选Leaflet + OpenStreetMap
* 决定：使用Leaflet + OpenStreetMap
* 原因：完全免费，无需API key，零运营风险，够用
* 否定方案：Google Maps（需绑定信用卡，有超额收费风险）

**2026-04-26** · 工具分工确定
* 决定：Claude负责设计决策与界面原型，Codex负责代码实现
* 原因：Claude擅长视觉决策，Codex擅长逻辑代码；Claude Artifact可直接生成原型供确认后交Codex实现

**2026-04-26** · 本地启动命令固定为--webpack
* 决定：package.json中dev命令固定为`next dev --webpack`
* 原因：项目存放在OneDrive目录，Turbopack与OneDrive路径不兼容

**2026-05-05** · 建立 Codex 驱动的文档管理流程
* 决定：用 Codex 桌面端直接读写本地 .md 文件并推送 GitHub，
  替代之前 AI 生成文档、用户手动上传的方式
* 原因：减少人工介入，实现对话切换时项目状态无缝衔接
* 否定方案：继续手动上传 GitHub（需要人工操作，容易遗漏）

**2026-05-05** · 建立项目快捷指令体系
* 决定：在 Claude 和 Codex 两侧分别定义快捷指令：
  /status（项目状态快照）、/review（对话回顾）、
  /update（生成/执行文档更新）、/decide（记录决策）、/next（下一步建议）
* 原因：规范协作流程，减少每次对话重复交代背景的成本
* 否定方案：无

**2026-05-05** · 建立 AGENTS.md 作为 Codex 永久记忆文件
* 决定：在仓库根目录放置 AGENTS.md，写入 Codex 工作规范、
  路径、指令定义，每次 Codex 启动自动读取
* 原因：避免每次对话重复发送背景信息
* 否定方案：每次对话重新发送完整背景提示词

**2026-05-08** · 飞书 API 对接方案确定
* 决定：在 app/api/events/route.ts 实现服务端代理，先获取 tenant_access_token，再分页拉取多维表格全量记录，格式化为前端友好的 JSON 字段后返回
* 原因：飞书 App Secret 只存在服务端环境变量，不能暴露给前端，必须走服务端代理
* 否定方案：前端直接调用飞书 API（CORS 报错 + Secret 泄露风险）

**2026-05-10** · noUiSlider CDN 源改为 unpkg
* 决定：noUiSlider 从 jsDelivr 换到 unpkg 加载
* 原因：jsDelivr 在当前开发环境加载失败，会阻塞地图初始化
* 否定方案：jsDelivr（加载不稳定）

**2026-05-10** · 油画图标放置路径确定为 public/icons/
* 决定：8张阶层图标 PNG 放在 public/icons/ 目录，文件名即阶层名称
* 原因：Next.js 静态资源标准路径，Leaflet DivIcon 可直接用 /icons/阶层名.png 引用

**2026-05-11** · 文档系统按"职责分层"重构
* 决定：合并 README-Verticalreading.md 入 AGENTS.md；文档按职责分层为协作层（AGENTS）、全局层（项目启动文档 + 决策日志）、专项层（设计规范 + 数据库文档）；总数从 6 减至 5
* 原因：原结构有大量重复（README 和 AGENTS 的指令定义、待办字段、commit 规范），每次开新对话上传文档负担重
* 否定方案：新增 SESSION_LOG.md + OPEN_QUESTIONS.md 两个文档管理跨对话衔接与开放问题（过度工程，反而让信息更碎）

**2026-05-11** · 文档更新规范确立
* 决定：在 AGENTS.md 第五章明确三套规则——颗粒度按文档类型分级（极粗到极细 6 档）；更新触发用客观条件而非主观重要性；维护按文档类型分类（累积型永不压缩、工作区型完成移除、规范型直接覆盖）
* 原因：原更新规则模糊，每次都靠 AI 临场判断，导致颗粒度不一致、漏更、不同段落写法不统一
* 否定方案：依靠"主观判断重要性"决定要不要更新（不稳定，每个 AI 的判断标准都不一样）

**2026-05-11** · 当前待办引入状态机和统一字段
* 决定：项目启动文档的「当前待办」从 5 行表格升级为子任务级条目；引入 💡 想法 → ⏳ 进行中 → ✅ 完成 三状态；每条待办强制 6 个统一字段（提出、当前进度、子任务、优先级、阻塞、下次接手提示）
* 原因：粗颗粒度待办承载不了"具体讨论到哪一步"这层细节，是细节丢失的主要原因；引入"想法"状态后，临时起意的念头也有地方装，从想法到完成自然演进
* 否定方案：新增 OPEN_QUESTIONS.md 文档专门管开放问题（增加文档数量、违背精简方向）

**2026-05-11** · /review 加 git log 对照
* 决定：/review 执行前先要求用户提供 Codex 端 `git log --oneline -20` 和 `git status` 输出，Claude 据此做"对话讨论 vs 代码实际"的差异检查
* 原因：原 /review 只能基于对话上下文回顾，看不到 Codex 实际改了什么代码，容易遗漏"讨论了但没推"或"推了但没讨论"的偏差
* 否定方案：Claude 自己 fetch GitHub 验证代码状态（实测 web_fetch 有 Anthropic 侧缓存层，读到的是旧版本）

**2026-05-11** · 放弃"Claude 自动读 GitHub"方案
* 决定：手动上传 5 个 MD 文档作为新对话开场的不可避免步骤；优化重点转向 review/update 流程本身的紧凑度
* 原因：web_fetch 工具有 Anthropic 侧缓存层，github.com 网页、raw.githubusercontent.com、api.github.com 三种 URL 都绕不开缓存（实测得到的决策日志比实际版本旧半个月）；Claude 在 claude.ai 网页端无法读本地文件（不像 Codex 是本地进程）
* 否定方案：用 GitHub Contents API 绕缓存（被 web_fetch 工具策略拦截）；让 Codex 输出"今日简报"代替上传 MD（简报只能装动态状态，无法替代产品定位、设计规范等静态背景知识）

**2026-05-11** · 删除 /decide 指令
* 决定：从快捷指令系统中删除 /decide
* 原因：功能完全被 /update 覆盖；对话中自然语言陈述决策即可，/review 会主动追问"否定方案"字段
* 否定方案：保留 /decide 作为"对话中立即格式化"工具（功能冗余，增加指令记忆负担）


**2026-05-12** · 设计 Token 系统接入（Commit 1）
* 决定：把 Claude Design 产出的完整 token 系统作为前端的 source of truth，新增 `app/tokens.css`（208 行），`app/globals.css` 顶部 `@import "./tokens.css"`，旧 9 个变量改写成 alias 形式过渡。三处技术改造：删除原 `@import` 字体加载、字体变量改引用 next/font 注入、token 全部放 `:root`
* 原因：原 globals.css 的变量定义不完整，缺少 spacing、shadow、blur 等系统化值；Design 产出经过 Codex review，兼容 Tailwind 4 + next/font；alias 过渡保证组件代码零修改
* 否定方案：增量式合并（保留旧变量并行，最后变量越积越多）

**2026-05-12** · 字体加载策略保持 next/font 独占
* 决定：tokens.css 不定义字体 family，`--font-display` / `--font-body` / `--font-zh` 三个变量完全由 `app/layout.tsx` 的 next/font/google 注入
* 原因：next/font 自托管字体是 Next.js 最佳实践；如果 tokens.css 也定义同名变量会自引用失效，覆盖会破坏自托管机制；变量名天然一致，无需转换
* 否定方案：把 next/font 注入变量名改成 `--font-playfair` 等，再让 token 变量 alias 到它们（违反"只改 tokens.css 和 globals.css"的本批硬约束）

**2026-05-12** · 推倒重写前端
* 决定：放弃在现有 globals.css 上打补丁迁移 token，改为在 redesign 分支上完全重写前端。保留：API 层（`app/api/events/route.ts`）、`app/layout.tsx`、`public/icons/`、所有项目配置文件、所有 MD 文档。重写范围：`app/globals.css`、`app/page.tsx`，按 components.jsx 的组件边界拆分到 `components/` 目录
* 原因：第二轮事实核查扫描发现现有 globals.css 是大量硬编码堆出来的——46 处硬编码颜色 + 45 处硬编码字号/圆角/间距，全部集中在 globals.css；组件代码 `.tsx/.ts` 里 0 处 `var(--xxx)` 引用，旧 9 个变量在样式层引用 32 次，其余全为硬编码；6 个 ink 附近暗棕派生色（`#33271b`、`#2d241a`、`#5f4725`、`#3a2d20`、`#3b2d1c`、`#5a4525`）和 1 个浅色 `#e9e1cf` 在 token 系统里无对应。"接入新 token"的真实工作量等同于重写，重写后代码结构和 Claude Design 产出对齐，未来维护成本更低
* 否定方案：保守迁移路线（把硬编码值逐个替换成 token 引用，工作量大、改完结构仍旧、未来还想重构）

**2026-05-12** · 重写前归档策略
* 决定：在 main 分支打 tag `v0.1-pre-redesign` 作为重写前里程碑（指向 commit `067d305`）；建 `redesign` 分支做重写工作；main 分支保持线上 vercel 版本不受影响；旧代码不删除、不物理归档，依赖 git 历史保留
* 原因：Git 历史天然保留旧代码；分支隔离让线上 vercel 版本不受影响；tag 作为重写起点便于未来回溯
* 否定方案：建 `legacy/` 或 `archive/` 目录物理归档旧文件（多余、让仓库变乱，Git 已经做了这件事）

**2026-05-12** · Claude Design 产出归档入仓
* 决定：把 Claude Design 产出的 16 个文件（含设计 token、组件代码、设计文档、6 张界面截图）归档到仓库 `design-reference/` 目录，作为重写阶段 Codex 的事实凭据
* 原因：这些文件之前只在用户本地，归档入仓后 Codex 重写时可直接读取；作为设计参考存档，未来追溯有据
* 否定方案：让用户每次开新对话上传给 Claude（资源没有版本控制、易丢失）

**2026-05-12** · 首页书封面图本地化
* 决定：6 张首版书封面下载到本地 `public/covers/`，未来 books.js 改引用本地相对路径（如 `/covers/don-quixote.jpg`），不沿用 Wikimedia URL 热链
* 原因：避免 Wikimedia 热链限制、加载速度受其服务器影响、对方图片如被删则首页崩
* 否定方案：直接用 Wikimedia URL（有热链风险）

**2026-05-12** · 上一阶段 /review 信息盲区与补救方案
* 决定：承认 2026-05-12 的 /review 和 /update 存在系统性疏漏，未对 design-reference/ 内部 16 个文件做盘点和口径统一，导致下一阶段开工前出现 19 个基础口径问题（涉及 source of truth、组件结构、设计参考之间冲突、未进 token 的硬编码色、首页书墙 style 分配、Landing H1 字号冲突等）。补救方案：下次新对话开工前由 Claude 先建立一份 `VerticalReading_前端重写参考.md`，作为 design-reference 内部口径 + 与现有规范冲突点的统一回答文档，归档后再启动重写方案设计
* 原因：今天把 design-reference 当"资产入库"处理（确认存在、commit、push 即完事），而非"重写阶段的设计契约"——应当读完每个文件、把它们之间的口径关系搞清楚、把和 v0.1 现有规范的冲突点全部列出来。在密集决策的对话末尾继续硬塞回答只会重复同样的盲区，留给下次完整对话集中处理更稳
* 否定方案：在本次对话尾声直接尝试回答 19 个问题（疲劳决策、信息密度过载会重复今天的盲区）；让下次对话自然涌现这些问题（已证明会一路追问、效率低、易遗漏）

**2026-05-13** · 阶段 3 前端重写完成
* 决定：在 redesign 分支完成 Parallels 全套前端重写，对照 Claude Design Standalone HTML 实施。改动范围：tokens.css（补字体变量、新增 disc token、H1 字号 92px、删除 .p-* 段）、globals.css 重写、page.tsx 重构，新建 7 个组件（TopNav / Landing / MapStage / FilterPanel / EventsPanel / Timeline / EventPopup）和 3 个数据文件（books.ts / world-events.ts / types.ts）。本地构建（`npx next build --webpack`）、生产代码范围 ESLint（`npx eslint app --ext .ts,.tsx`）通过，浏览器走查通过。redesign 分支已 push，等待合并回 main 触发 Vercel 部署。
* 原因：阶段 2 v0.1 的 globals.css 硬编码堆积，且与 Claude Design 新设计语言冲突；本次重写一次性对齐新设计，告别打补丁迁移。
* 否定方案：无

**2026-05-16** · 大陆访问方案选 Vercel + 自定义域名
- 决定：大陆用户访问采用 Vercel 继续部署、自定义域名 CNAME 指向 `cname-china.vercel-dns.com` 的方式
- 原因：无需迁移部署平台，只改 DNS 配置；个人使用阶段成本最低，够用
- 否定方案：迁移到腾讯云/阿里云国内部署（需 ICP 备案、运维复杂度大幅上升，当前阶段不值得）

**2026-05-16** · 地图瓦片按用户 IP 自动切换双源
- 决定：地图底图按用户 IP 自动切换 tile 源——大陆用户走高德瓦片，其他地区用户走 OSM
- 原因：单一 OSM 大陆加载不出；单一高德境外访问慢且条款不友好；双源切换兼顾两侧用户，Leaflet 切换 tile URL 技术实现简单
- 否定方案：单用高德（境外不友好）；单用 OSM（大陆被墙）；OSM + CDN 中转（技术复杂，不适合当前阶段）

**2026-05-16** · 历史地图底图技术方案确定
- 决定：使用 David Rumsey Georeferencer WMTS tile 服务，通过 Leaflet `L.tileLayer()` 加载历史古地图叠加层
- 原因：清晰度高、已做地理校正无需手动坐标、大陆可直接访问、无需用户登录验证、零成本
- 否定方案：ImageOverlay + 本地JPG（清晰度受限、大图加载慢）；Stamen Watercolor（现代数据渲染，非真实古地图）；CSS滤镜做旧（视觉欺骗）；OpenHistoricalMap（仅历史边界数据，视觉气质弱）

**2026-05-16** · 节点1-3早期古地图暂缓
- 决定：唐（~650）、宋（~1100）、元（~1300）三个节点暂不实现
- 原因：Wikimedia 可用图片清晰度差、效果不佳；David Rumsey 无对应早期全球地图
- 否定方案：低清晰度 JPG 凑合上线（影响产品品质）

---

## 流程决策

**2026-05-12** · 新对话开工前置流程升级
* 决定：下次新对话开始时，流程从原来的"上传 5 个 MD → /status → 输出重写方案设计"升级为"上传 5 个 MD + 19 问题清单 → Claude 逐题回答（必要时要求补充文件，如 app.css、events.js、preview/ 目录）→ 整理成 VerticalReading_前端重写参考.md → Codex 归档 → 才开始重写方案设计"。该参考文档作为重写开工的硬前置条件
* 原因：design-reference/ 内部有多份相关但不一致的文件（如 components.jsx 两份、colors_and_type.css 与 tokens.css 字体定义差异、app.css 含 components.jsx 未体现的新设计、`.p-*` 语义类无处使用等），不先做口径统一就开始重写会反复返工
* 否定方案：直接开始重写方案设计，遇到问题边写边问（今天已证明这会一路追问，效率极低）


---

## 设计与归档相关

**2026-05-12** · 前端重写参考文档建立 8 项决策
* 决定：在《VerticalReading_前端重写参考.md》中锁定 8 项基础口径——source of truth、tokens 改造、性别圆盘、H1 字号、History tab 数据、开工分支策略、Atlas FAB 图标
* 原因：上一阶段 /review 发现 19 个未澄清的基础问题阻塞重写方案设计，本次对话逐题决策
* 否定方案：（具体内容详见参考文档，此处不重复）
