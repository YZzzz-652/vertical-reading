# Vertical Reading · AI 协作规范

本文档是 Claude 和 Codex 两个 AI 协作者的工作规范。两个 AI 共同遵守通用部分，差异化部分分别遵守对应章节。

---

## 一、项目背景

- **产品定位**：一个"垂直穿透读书"的文学可视化平台，让用户用上帝视角看到不同作品里的人物在同一时空下的平行命运。
- **GitHub 仓库**：https://github.com/YZzzz-652/vertical-reading
- **本地项目路径**：/Users/yizhongwang/Library/CloudStorage/OneDrive-共享的库-Onedrive/VibeCoding Project/Vertical Reading/vertical-reading
- **产品细节、技术栈、当前进度**：见《项目启动文档》

---

## 二、硬性约束（通用·两个 AI 都必须遵守）⚠️

1. 不修改飞书表格字段名、单选字段合法值列表
2. 不写入任何 App Secret 或凭证到代码文件
3. 不确定的修改先输出方案确认，不直接执行
4. **Claude 不输出代码改动指令**（所有代码改动由 Codex 负责）
5. 代码文件（.ts .tsx .js .jsx .css .json 等）的修改权归 Codex
6. MD 文档的修改权两个 AI 共同维护（Claude 生成指令，Codex 执行）

---

## 三、AI 协作分工

- **Claude**：设计决策、对话回顾、生成文档更新指令；不直接改文件，只生成指令交给 Codex 执行
- **Codex**：执行 Claude 生成的更新指令、改 MD 和代码、git commit、git push
- **共同遵守**：本规范全部内容

---

## 四、文档结构与职责分层

项目共 5 个核心文档（数据生产完成后数据库文档归档，减为 4 个）：

| 文档 | 职责 | 类型 |
|---|---|---|
| **AGENTS.md** | AI 协作规范（本文件） | 规范型 |
| **VerticalReading_项目启动文档.md** | 产品定位、技术栈、Roadmap、当前待办、已知问题 | 混合型 |
| **VerticalReading_决策日志.md** | 历史决策记录（含否定方案） | 累积型 |
| **VerticalReading_设计规范.md** | 视觉规范（字体、颜色、组件） | 规范型 |
| **VerticalReading_Database_启动文档.md** | 数据生产规范、书目清单 | 累积型 |

---

## 五、文档更新规范（通用·两个 AI 都遵守）

### 5.1 颗粒度规则

同一文档内部颗粒度保持一致，需要更细颗粒度时找对应文档/段落，不要把细节硬塞进粗颗粒度的地方。

| 文档/段落 | 颗粒度 | 示例 |
|---|---|---|
| AGENTS.md | 极粗 | "新增 /brief 指令"这一级，不到指令实现细节 |
| 项目启动文档 · 技术栈 | 粗 | "使用 Leaflet"，不到具体配置 |
| 项目启动文档 · Roadmap | 中（组件级） | "时间轴完成"，不到子任务 |
| 项目启动文档 · 当前待办 | 细（子任务级） | "气泡卡片关闭按钮位置已选方案 B" |
| 决策日志 | 中 | 一条决策一个条目，含原因+否定方案 |
| 设计规范 | 极细 | 精确值（72px、#F5EDD8） |
| 数据库文档 | 中 | 单本书状态、字段级定义 |

### 5.2 更新触发条件

不要凭"重要性"主观判断，按客观触发条件更新：

- **AGENTS.md**：仅当协作流程、指令定义、文档结构变化时更新。日常工作不动。
- **项目启动文档 · Roadmap**：阶段或组件状态变化时更新（"进行中"→"已完成"）
- **项目启动文档 · 当前待办**：每次对话结束时检查，状态推进、新增待办、完成移除
- **项目启动文档 · 技术栈**：仅在选型决策变化时更新
- **决策日志**：每次对话产生新决策时追加。所有改变项目方向的判断都算决策，包括"否定某方案"本身。
- **设计规范**：仅当视觉规范变化时更新（新增组件、调整精确值）
- **数据库文档**：仅当数据生产状态变化时更新（某本书完成）

**重要原则**：对话里讨论了但没产生决定的事情不进文档。悬而未决的放到"当前待办"的"待决定"子项里，不进决策日志。

### 5.3 文档维护规则

不同文档按不同方式维护：

- **累积型文档**（决策日志、数据库文档）：永不压缩、永不删除历史条目。只追加。条目超过 50 条后可加目录索引段，但条目本身不动。
- **工作区型段落**（项目启动文档 · 当前待办）：任务完成后从待办移除，关键完成事项可追加到决策日志。
- **混合型段落**（项目启动文档 · Roadmap）：已完成阶段保留 ✅ 标记，体现演进路径，不删除。
- **规范型文档**（AGENTS、设计规范）：规则更新时直接改，旧规则的变化记入决策日志。

**任何文档都不主动做"瘦身压缩"。看起来变长不是问题，看起来变乱才是问题。乱了的解决方法是加索引/分段，不是删内容。**

### 5.4 核心原则

> 粗的部分稳定不动，细的部分随对话演进，历史一律保留。
>
> "清晰"不等于"短"。文档长不是问题，文档乱才是问题。

---

## 六、快捷指令定义（通用·两个 AI 都遵守）

### /status
输出项目当前快照：
- 当前阶段（数据生产 / 前端开发 / 部署）
- 数据进度：已完成 X 部 / 41 部，共 XX 条
- 前端进度
- 当前待办摘要
- 下一个优先任务

### /review
**执行前**：先要求用户提供 Codex 端 `git log --oneline -20` 和 `git status` 的输出。

**回顾结构化输出**：
- 本次完成的事（对话讨论 + commit 证据双重确认）
- 做出的决定（格式化，强制含"否定方案"字段，没否定的主动追问）
- 对话讨论了但没产生 commit 的事（提醒用户是否漏推）
- 遗留未决事项

### /update
基于本次对话内容，生成标准文档更新指令。
**规则：只输出本次对话涉及变化的【】块，没有变化的不输出。**

输出格式：

```
## Vertical Reading 文档更新指令

请按以下内容更新文档并推送到 GitHub，每个文件单独一次 commit。

【VerticalReading_决策日志.md】
在「[对应分类]」下新增：
[日期] · [标题]
- 决定：
- 原因：
- 否定方案：（无则不写）

【VerticalReading_项目启动文档.md】
将「[段落名]」部分更新为：
[新内容]

【VerticalReading_Database_启动文档.md】
在书目表中：
- 将《书名》状态改为 ✅已完成，条数填 XX 条

【VerticalReading_设计规范.md】
将「[段落名]」部分更新为：
[新内容]

【代码提交】
检查未提交改动：git status
有改动则按规则提交：
- 功能新增：feat: [简述]
- 样式调整：style: [简述]
- 修复问题：fix: [简述]

完成后列出每个文件的 commit hash，确认 push 成功。
```

### /decide [决策内容]
将用户描述的决定格式化为决策日志标准格式：
```
[今日日期] · [标题]
- 决定：
- 原因：
- 否定方案：（无则不写）
```

### /next
基于当前项目状态，输出优先级最高的 3 件事，说明排序理由。

---

## 七、Claude 专属规则

### 启动流程
每次新对话开启时：
1. 等待用户上传 5 个 MD 文档（AGENTS + 项目启动文档 + 决策日志 + 设计规范 + 数据库文档）
2. 用户同时粘贴 Codex 端 `git log --oneline -10` 输出
3. 读完后输出 /status 快照
4. 等待指令

### 行为边界
- **不输出代码改动指令**，所有代码改动转交 Codex 处理
- 如用户要求修改代码，回复"代码改动应由 Codex 执行"，并草拟给 Codex 的指令
- 可在对话中贴代码片段供讨论，但不让 Codex 直接落地，需用户确认后才转 Codex
- 不主动 fetch GitHub 文档（实测有缓存问题，以用户上传的版本为准）

### 对话结束流程
对话结束前必须依次执行：
1. /review（含 git log 对照）
2. /update（生成更新指令）
3. 提示用户复制指令给 Codex 执行

---

## 八、Codex 专属规则

### 启动流程
1. `git pull origin main`（拉取最新）
2. 读取仓库所有 MD 文档
3. 简要告诉用户当前项目状态，等待指令

### 文件操作范围
- 可改：所有 MD 文档、所有代码文件（.ts .tsx .js .jsx .css .json 等）
- 严禁：飞书表格字段结构、单选合法值列表、写入 App Secret

### 接收 /update 指令后的处理流程
1. 读取需要修改的文件，确认当前内容
2. 按【】块逐文件修改
3. 每个文件单独一次 commit：`git add [文件名] → git commit → git push origin main`
4. 不合并多文件 commit
5. 回报：修改了什么、commit hash、push 是否成功

### Commit message 规范
- 文档：`docs: [文档名简称] - [简述]`
- 代码功能：`feat: [简述]`
- 代码样式：`style: [简述]`
- 代码修复：`fix: [简述]`
- 配置/杂项：`chore: [简述]`

### 硬约束
- 没有出现在【】块里的文件不动
- 不确定的修改先输出方案确认，不直接执行

---

## 附录·历史变更

- 2026-05-11：合并原 README-Verticalreading.md 内容；新增文档更新规范段；调整章节结构（通用前置、差异后置）；升级 /review 加 git log 对照、/update 明确按需输出规则
